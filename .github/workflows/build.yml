name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: '버전 번호 (예: 1.1.0)'
        required: false

jobs:
  build:
    strategy:
      matrix:
        os: [macos-latest, windows-latest]
        include:
          - os: macos-latest
            build-command: npm run build:mac
          - os: windows-latest
            build-command: npm run build:win
    runs-on: ${{ matrix.os }}
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Check environment variables
        shell: bash
        run: |
          if [ -z "${{ secrets.MASTER_ADMIN_PASSWORD }}" ]; then
            echo "⚠️ MASTER_ADMIN_PASSWORD가 설정되지 않았습니다. 기본값을 사용합니다."
          else
            echo "✅ MASTER_ADMIN_PASSWORD가 설정되었습니다."
          fi
      
      - name: Build app
        run: ${{ matrix.build-command }} --publish never
        env:
          # 게시(Publish)를 명시적으로 비활성화하여 오류를 방지합니다.
          ELECTRON_BUILDER_PUBLISH: never
          
          CSC_IDENTITY_AUTO_DISCOVERY: false
          MASTER_ADMIN_PASSWORD: ${{ secrets.MASTER_ADMIN_PASSWORD }}
          PUBLISH: never
          GH_TOKEN: "" # GitHub 토큰 비활성화
          GITHUB_TOKEN: "" # GitHub 토큰 비활성화
          CI: "true"
          ELECTRON_BUILDER_CACHE: ""
          ELECTRON_SKIP_BINARY_DOWNLOAD: "1"
      
      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dist-${{ matrix.os }}
          path: dist/
          retention-days: 30
