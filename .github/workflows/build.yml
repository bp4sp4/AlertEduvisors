name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: '버전 번호 (예: 1.1.0)'
        required: false

jobs:
  build:
    strategy:
      matrix:
        os: [macos-latest, windows-latest]
        include:
          - os: macos-latest
            build-command: npm run build:mac
          - os: windows-latest
            build-command: npm run build:win
    runs-on: ${{ matrix.os }}
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Check environment variables
        shell: bash
        run: |
          if [ -z "${{ secrets.MASTER_ADMIN_PASSWORD }}" ]; then
            echo "⚠️ MASTER_ADMIN_PASSWORD가 설정되지 않았습니다. 기본값을 사용합니다."
          else
            echo "✅ MASTER_ADMIN_PASSWORD가 설정되었습니다."
          fi
      
      - name: Generate config.bin
        shell: bash
        run: node inject-env.js
        env:
          MASTER_ADMIN_PASSWORD: ${{ secrets.MASTER_ADMIN_PASSWORD }}
      
      - name: Verify config.bin
        shell: bash
        run: |
          if [ -f "config.bin" ]; then
            echo "✅ config.bin 파일이 생성되었습니다."
            # 파일 크기 확인 (플랫폼별로 다른 명령어 사용)
            if command -v stat >/dev/null 2>&1; then
              if stat -c%s config.bin >/dev/null 2>&1; then
                echo "파일 크기: $(stat -c%s config.bin) bytes"
              elif stat -f%z config.bin >/dev/null 2>&1; then
                echo "파일 크기: $(stat -f%z config.bin) bytes"
              else
                echo "파일 확인됨"
              fi
            else
              echo "파일 확인됨"
            fi
          else
            echo "❌ config.bin 파일이 생성되지 않았습니다!"
            exit 1
          fi
      
      - name: Build app
        shell: bash
        run: |
          # 모든 GitHub 관련 환경 변수 완전 제거
          unset GH_TOKEN GITHUB_TOKEN 2>/dev/null || true
          export GH_TOKEN=""
          export GITHUB_TOKEN=""
          export PUBLISH=null
          export ELECTRON_BUILDER_PUBLISH=null
          
          # electron-builder 네트워크 요청 완전 차단
          export ELECTRON_BUILDER_CACHE=""
          export ELECTRON_BUILDER_ALLOW_UNRESOLVED_DEPENDENCIES=false
          export ELECTRON_GET_USE_PROXY=false
          export ELECTRON_SKIP_BINARY_DOWNLOAD=1
          
          # config.bin 파일 확인
          echo "📦 빌드 전 파일 확인:"
          if [ "${{ matrix.os }}" == "windows-latest" ]; then
            if [ -f "config.bin" ]; then
              echo "✅ config.bin 파일 확인됨"
            else
              echo "⚠️ config.bin 파일을 찾을 수 없습니다"
              exit 1
            fi
          else
            ls -la config.bin || echo "⚠️ config.bin 파일을 찾을 수 없습니다"
          fi
          
          # 플랫폼별 electron-builder 직접 호출 (npm run 스크립트 우회)
          # --publish never를 두 번 명시하여 확실히 차단
          echo "🔨 빌드 시작..."
          echo "현재 디렉토리: $(pwd)"
          echo "필요한 파일 확인:"
          test -f "package.json" && echo "✅ package.json" || echo "❌ package.json 없음"
          test -f "config.bin" && echo "✅ config.bin" || echo "❌ config.bin 없음"
          test -f "main.js" && echo "✅ main.js" || echo "❌ main.js 없음"
          
          if [ "${{ matrix.os }}" == "macos-latest" ]; then
            echo "macOS 빌드 실행 중..."
            # publish를 null로 명시적으로 설정하여 모든 네트워크 요청 차단
            if npx electron-builder --mac --config.publish=null; then
              echo "✅ macOS 빌드 성공"
            else
              echo "❌ macOS 빌드 실패"
              exit 1
            fi
          else
            echo "Windows 빌드 실행 중..."
            # publish를 null로 명시적으로 설정하여 모든 네트워크 요청 차단
            if npx electron-builder --win --config.publish=null; then
              echo "✅ Windows 빌드 성공"
            else
              BUILD_EXIT_CODE=$?
              echo "❌ Windows 빌드 실패 (exit code: $BUILD_EXIT_CODE)"
              echo "오류 상세 정보를 확인하세요."
              exit $BUILD_EXIT_CODE
            fi
          fi
          echo "✅ 빌드 완료"
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: false
          MASTER_ADMIN_PASSWORD: ${{ secrets.MASTER_ADMIN_PASSWORD }}
          PUBLISH: null
          ELECTRON_BUILDER_PUBLISH: null
          ELECTRON_BUILDER_CACHE: ""
          ELECTRON_BUILDER_ALLOW_UNRESOLVED_DEPENDENCIES: "false"
          ELECTRON_GET_USE_PROXY: "false"
          ELECTRON_SKIP_BINARY_DOWNLOAD: "1"
          GH_TOKEN: ""
          GITHUB_TOKEN: ""
          CI: "true"
      
      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dist-${{ matrix.os }}
          path: dist/
          retention-days: 30